# EnjoyIot GitBook target

Use this profile only for the EnjoyIot SaaS API documentation.

- Organization ID: `3PDTWkWHxUDpogC9f56l`
- Site: `ebelong` (`site_Sa3ga`)
- API Reference space ID: `qK8ynFtx4b30MZy26mrl`
- Stable public URL: `https://tiks-organization-2.gitbook.io/ebelong/api-reference/`

Existing GitBook OpenAPI specification names:

- `saas-v1-space-management` — space endpoints
- `saas-v1-other-interfaces` — all other SaaS business endpoints
- `saas-v1-authentication` — authentication endpoints

The API Reference navigation uses Chinese top-level groups: `通用接口` and `应用接口`. Preserve legacy pages, including the old OpenAPI example, unless an explicit deletion request names them.

After a merge, query the site configuration and current revision before reporting success. Return both the stable URL and a revision-pinned URL. If the stable page appears stale, recommend a hard refresh or an incognito window; do not create a second site or duplicate space merely to work around cache.
